"""nina-scan: source code scanner that generates NINA action manifests.

Runs entirely on the merchant's machine. No source code is uploaded —
only the generated manifest (route structure, methods, auth flags) leaves
the machine.
"""
